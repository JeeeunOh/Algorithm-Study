#include <iostream>
#include <queue>
using namespace std;

int main(){
    int T;
    cin >> T;

    while(T--){
        int N,M;
        int cnt=0;
        int res=0;
        cin >> N >> M;

        while(M--){
            int a,b;
            cin >> a >> b;
        }

        cout << N-1 << '\n';
    }
}